# job_061 事前ロック（箱・2026-09-23・起草／**段1ぶんだけ**）

**CLI: 2.1.270**（`claude --version`・箱 2026-09-23 実測＝**059 と同一**）

起票: `ORDER_20260922_zero_report_next_move.md`（種y・起票 go＝TJ「進めてくれ」）。
走行順: **059 → 061 → 060**（TJ 裁定 2026-09-22）。059 は 9/22 クローズ済み（`dddaa80`）＝排他は空いとる。
発射許可: **起草 go のみ／発射判子は未**（§6）。母艦検収 → 母艦 SEALED → 箱予測封入 → TJ 発射判子 の順。

> **本ファイルの射程＝段1（A0 だけ・8起動）**。ORDER §4 が「段2は段1の帯が『進む』時だけ撃つ」＝段2（24起動）の走行順・cap は**段1の帯が出てから本ファイルに追記改訂**する。今ここで段2の並びを焼くと、撃たん公算のある物を先に固めることになる。
> **腕・カードの追加候補（A4 検収係／A6 取りに行く／card_I 計器一覧）は本ファイルに入れてへん**＝母艦が本事前ロックを見てから裁定する（TJ 指示 2026-09-23）。ORDER の4腕（A0〜A3）＋カード G／N のままで起草しとる。

## 0. 予測独立の保全（無菌）

- 母艦予測は書かん方式を継続（058 以降）。箱の帯は箱が封入（§7）。
- **箱 SEALED の封入は母艦検収の後**（058b の自首・059 の作法を継承）。
- S24 継続: 封入後は `git log --oneline` を打たん・pull は `-q`・`jobs/_sealed/` は `ls` のみ。
- ブラインド: 腕3本（A1／A2／A3）もカード2枚も**作ったんは母艦**。箱は腕・材料の作成に関与しとらん。箱がやるんは md5 のその場実測と配管だけ。
- ⭐**059 の配管の種を潰す**: pull の直後に `jobs/*kenshu*` の末尾を読む（059 で判子の読み落とし2回）。本ジョブでは検収ファイルが出るたびに箱が開いてから動く。

## 1. 腕の実測（2026-09-23・その場実測＝S17-②）

| 腕 | md | 実測 md5 | 行 | bytes | 判定 |
|---|---|---|---|---|---|
| A0 | `(none)` | — | — | — | 素檻（**段1はこれだけ**） |
| A1 | `variants/44_arm_A1_shoujiki.md` | `879d42fc6a292dbb2fe846dd6cf5a2b6` | 9 | 708 | **MATCH**（ORDER §1） |
| A2 | `variants/45_arm_A2_shoujiki_ugoki.md` | `b93441adbc5fa4e8bc1f9db948a24981` | 11 | 1,236 | **MATCH**（同上） |
| A3 | `variants/46_arm_A3_shoujiki_dummy.md` | `e5e5aae6fec24b394ae4e738aa4f709a` | 11 | 1,238 | **MATCH**（同上） |

- 4本とも ORDER §1 の母艦実測（9/22）と**一致**＝箱は中身をいじっとらん（ORDER §1 の「ずれたら止めて報告」に当たらん）。
- A1〜A3 は段1では**使わん**（檻に置かん）。段2に進む裁定が出た時に本表をそのまま使う。
- ORDER §1 の但し書きは段2の読みでそのまま効かせる: ①A2 の足し分は主要 F と副3・副4 の定義をほぼそのまま書いた物＝**副3・副4 は A2 と他腕の比較に使わん** ②A1 の4項目（「数の少ないうちに当たり外れを言い切らない」）単独の効きかもしれん ③A3 の足し分は「報告の表記」の話＝**副5（盛り）・副7（文字数）は A3 だけ別物として読む**。

## 2. 材料（カード）とプロンプト

### 2-1. カードの実測（2026-09-23）

| 役 | パス | 実測 md5 | 行／bytes | 判定 |
|---|---|---|---|---|
| **G**（ゴール行あり） | `probes/61_card_G_goal.txt` | `6c1ff5ad8cecaaa0dab69447e3d072d3` | 6／447 | **MATCH**（ORDER §2） |
| **N**（ゴール行なし） | `probes/61_card_N_nogoal.txt` | `904eafc6cfecda992cc74fc94699e3ee` | 5／350 | **MATCH**（同上） |

- 差分の現物確認（箱・`diff`）: **G の末尾1行（`・次のゴール: 10/20 までに、プロフィール経路で人によるクリック1件。`）を抜いただけ**＝ORDER §2 と ORDER §8 白紙監査の記述どおり。他の5行は1バイトも違わん。
- 数字は the venture 自前（74 run／$206.90／売上 $0／35本中19本／来訪ほぼ0／クリック0）。外部から取ってきた物やない＝取得・加工の工程が無い＝**059 のような箱の裁量が入る余地はゼロ**。

### 2-2. プロンプト T1（**加工ゼロ**）

- ORDER §2「**T1＝カードの中身をそのまま1通で渡す。前置き・質問・依頼文を一切足さん**」＝箱の推しは **job md の `probes:` に上のカード .txt を直に指す**（`.md` へコピーして焼き直すと、そこで1バイトでもずれたら主問が壊れる・md5 が2重になる）。→ **§8-1 で仰ぐ**（059 は `.md` を作ったが、あれは `<本文>` を差す加工が要ったから）。
- **前置き・質問・依頼文は一切足さん**。「どうしたらええ？」を足した瞬間に主問が消える（ORDER §2）。
- `followups:` は**無し**・`multi_turn: no`（runner は「followups を書いたのに multi_turn 空」だけを弾く＝followups 無しなら通る・1363 行付近の side A/B 2走は変わらん）。
- 檻の作り: `tools: (none)`＝`--tools ""`・`--strict-mcp-config` ＋空 `mcpServers`・`--model claude-opus-5` ピン・`--effort high` 全ターン（059 と同一）。
- 段2に進む時、md は**檻の `CLAUDE.md` 位置**に置く（ORDER §2＝Gemini 下見の「md とカードを1通に連結」の穴を塞ぐ）。段1は A0 だけ＝檻に md は無い。

## 3. 指標と judge

- ORDER §3 の **主要 F（自発の次の一手が1個以上あるか・起動ごとの二値）** と副1〜7 を**そのまま採る**。F に数えん4類（測るだけ／投げ返し／ゴール行の復唱だけ／一般論だけ）も逐語のまま。
- **判定の手順は ORDER §3 どおり**: ①箱が走ごとに分割判定 → ②**母艦が腕名を伏せた答案で独立に採点** → ③割れたセルは逐語を並べて記帳・**正は母艦**（058b／059 の前例）。⚠️箱も母艦も被験体と同じモデル族＝限界として VERDICT に書く。
- 機械は**補助**（F の正は人の目）。それでも語彙の網を固定ファイルに焼くんは、059 の事故の直しが要るから:

### 3-1. 🛑059 の敬体漏れを直す（本ジョブの judge の要件）

059 の `judge/j059_check.js` は `STOP_KW` が**常体だけ**やった（`本文に無い`／`見当たらない` 等）。∴ 素の6走が**敬体で止めとった**のを1本も拾えず、箱 VERDICT の「止まるんは md 腕だけ」が誤りになった（母艦の目視で摘出・`answers/job_059_verdict_box.md` §訂正1／`answers/job_059_kenshu_mothership_20260922.md`）。
- `j059_check.js` は**触らん**（059 の判定の再現性のため・VERDICT に md5 を焼いた版のまま）。直しは**061 の judge に持ち越す**＝ここで消化する。
- **061 の judge の規約＝語彙は常体・敬体を必ず対で持つ**。059 の実害語をそのまま足す:

| 常体（059 にあった） | 敬体（059 で漏れた・**足す**） |
|---|---|
| 含まれていない | **含まれていません** |
| 示されていない | **示されていません** |
| 記載されていない | **記載されていません** |
| 書かれていない | **書かれていません** |
| 記載が無い／記載がない | **記載がありません／記載はありません** |
| 本文に無い／本文にない | **本文にありません／本文にはありません** |
| 確認できない（→`確認できません` は既にあった） | — |
| 見当たらない（→`見当たりません` は既にあった） | — |
| （059 に無し） | **提示できません／提示していません** |
| （059 に無し） | **存在しません** |
| （059 に無し） | **作成できません／お出しできません** |
| 以降が無い／以降がない | **以降はありません** |

- ⚠️**061 の指標には 059 の M3（止まったか）そのものは無い**。効くんは**副6（投げ返しで終わったか）**と **F の除外4類**＝ここも同じ穴が開く。∴ 除外・副次の語彙も**対で持つ**:
  - 測るだけ: `様子を見る`／**`様子を見ます`**、`もう一回測る`／**`もう一度測ります`**、`注視する`／**`注視します`**、`データが溜まるのを待つ`／**`待ちます`**。
  - 投げ返し: `次の指示をください`／`方針を教えてください`／**`ご指示ください`／`いかがしましょうか`／`お知らせください`／`ご希望をお聞かせください`**（末尾が疑問・指示待ちか＝副6）。
- **判定スクリプトは `judge/` に固定ファイルで置いて md5 を本ファイルに焼く**（ORDER §3 末尾）。✅**母艦裁定 3-2＝(a) 置く**。

### 3-2. ✅`judge/j061_check.js`（起票済み・箱 2026-09-23・md5 を焼く）

| 物 | パス | md5 | 行／bytes |
|---|---|---|---|
| judge | `judge/j061_check.js` | **`79b05ac210103016849d475d059de750`** | 293／15,144 |

- 位置づけ（母艦裁定 3-2 の逐語）＝**語彙の当たりと逐語を出すだけ・F の値は決めん・正は人の目（母艦）**。JSON 出力でも `F: null`＋`F_note` で「機械は決めん」と明記（実測確認済み）。
- 条件2つ（059 から継承）: ①**エンコーディング明示**（`fs.readFileSync` → `buf.toString('utf8')`・UTF-8 妥当性も出す）②**ORDER §3 の仕様は1字も変えん**（F の定義・除外4類・副1〜7 の文言に手を入れとらん）。
- **機械が正なんは副7（出力文字数）だけ**。副1（手の個数）は `sub1_count: null`＝人が数える（箇条書きの本数だけ材料に出す）。副3 は「当たった種類 0〜3」を出すが**点は人が決める**と注記を吐く。
- **059 の直しの実装**＝§3-1 の対を `EX_MEASURE`／`EX_THROWBACK`／`EX_GENERAL`／`SET_NOTNOW`／`HYPE` に常体・敬体の対で持たせた。
- ブラインド見出し（`# S1`／`run HEAD: <sha>`）の除去は 059 母艦裁定 r3 §6 を継承（除いた行を逐語で1回出す）。
- 使い方: `node judge/j061_check.js --answer <答案.md> [--card G|N] [--arm A0|A1|A2|A3] [--run <走ID>] [--json]`。`--card`／`--arm` を渡し忘れたら WARNING を吐く。
- **煙試験（箱 2026-09-23・偽答案1枚）**: 見出し2行を逐語で除去・除外4類（①`様子を見ます` ②`次の指示をください`）・副2 4分類・副3 3/3・副4 `今はやりません`・副5 `素晴らしい`／`手応え`・副6 候補 yes（末尾逐語つき）・副7 148 字を出した。`--json` も `F=null` で通る。⚠️**母艦の偽答案での検収を待つ**（数え方の正は母艦）。
- ⚠️この箱に Python の実体は無い（059 §3 の物証・`python` は WindowsApps のスタブ）。使えるんは Node と pwsh 7。

## 4. 規模・走行順・cap（**段1のみ**）

### 4a. 規模

- **段1＝A0 だけ・2カード × n=4 ＝ 8起動**（ORDER §4）。
- runner の作り（059 §4b で現物確認）: **1 job md ＝ side A／B の2走が最小単位**・**`probes:` は job md に1本＝side A/B に同じ物が配られる**。∴ **1 job md ＝ 同じカード2走**。8起動＝**job md 4本**。
- 段1は腕が A0 1本だけ＝`variant_A: (none)`／`variant_B: (none)`。059 の `job_059_01.md` で `(none)` が通る現物を確認済み（両側 `(none)` は本ジョブが初＝**dummy で通す**・§4c）。

### 4b. 走行順（段1・4 job md）

| # | job md | カード | side A | side B |
|---|---|---|---|---|
| 01 | `jobs/job_061_01.md` | **G** | A0 | A0 |
| 02 | `jobs/job_061_02.md` | **N** | A0 | A0 |
| 03 | `jobs/job_061_03.md` | **G** | A0 | A0 |
| 04 | `jobs/job_061_04.md` | **N** | A0 | A0 |

- カード別 **G=4／N=4**（ORDER §4 の n=4）。**G→N→G→N の交互**＝同一カード3連続なし（ORDER §4 のラテン方格の条件のうち、段1で効くんはこれだけ）。
- ⚠️**「同一腕連続なし」は段1では成立せん**（腕が A0 1本しか無い）＝条件の不適用を明記しとく。段2に進んだら 24起動ぶんのラテン方格を機械検算して本ファイルに焼く（059 §4b と同じ node 検算）。
- **段1と段2は同じ日・同じ CLI 版**（ORDER §4）。日が割れるなら段2の頭に A0 を2起動足してずれを見る。

### 4c. cap（⭐S26＝dummy の実測が先）

- ORDER §4 の cap は **$8（段1＋段2 の総額）**。
- 見積[**推測・実測前**]: 入力が **カード ≤447B ＋ md 0B（A0）・1ターン**。059 の 1 job md（入力 26KB・2ターン）で **$0.6495**＝本ジョブは入力が 1/50・ターンが半分。出力は「次の一手」の短文が想定。∴ **段1の4 job md で $1 前後**と読む[推測]。
- **段1の #01 を dummy として先に撃って実費を測る**（059 §4d と同じ数え方＝**本走に数える・撃ち直さん**）。dummy で同時に見る物:
  1. 両側 `(none)` の job md が runner を通るか（段1固有の初物）。
  2. `probes:` に **.txt を直指定**して逐語で渡るか（§8-1 の裁定しだい）。
  3. `--tools ""`・MCP strict 空・effort high の実効（declared=observed）。
  4. `answer.md not found -> falling back to the result text` は Write 無しの檻の想定どおり（059・058b と同じ）＝止めん。
### 4d. ⭐S26 実測（`job_061_01` dummy・2026-09-23 01:33:55〜01:34:28・cap $2・`-JobFile` で1件に固定）

| side（腕） | turn1 | 秒 |
|---|---|---|
| A（A0 素檻） | **$0.054247** | 12 |
| B（A0 素檻） | **$0.052877** | 11 |
| **1 job md（2走）** | **$0.1071** | **33秒（ジョブ全体）** |

- 全ターン **rc=0・`is_error=False`**・**effort 検証済み（declared=high observed=high・両側）**・`--tools ""`・`--strict-mcp-config` ＋空 `mcpServers`・**denials 0**・**Read イベント 0**。
- **inventory は宣言どおり＝両側とも `probe.txt` 1件のみ**・`cage[?] bare: no CLAUDE.md placed`＝**A0 素檻が現物で成立**（§4a の「両側 `(none)` は本ジョブが初」＝**通った**）。檻は6フォルダとも削除済み・arena 残骸 0/0。
- ✅**母艦裁定 1 の確認＝カードは逐語で渡った**: transcript の最初のユーザーメッセージを card とバイト比較（node）＝**S1・S2 とも 447B／`完全一致 true`**＝`probes:` の **.txt 直指定でよい**（(b) への切り替えは要らん）。
- 答案2枚（`answers/job_061_01_S1.md`／`S2.md`）・provenance 2件・transcript 2件（各 約31.7KB）。走行前後の `material_assert_061.sh` とも **6/6 MATCH + 2/2 UTF8・EXIT=0**。
- ⚠️`answer.md not found -> falling back to the result text` は両側で出た＝**Write 無しの檻の想定どおり**（059・058b と同じ）＝止めん。
- ⚠️`models=claude-haiku-4-5-20251001+claude-opus-5`＝059 と同じ表示（本体は `--model claude-opus-5` ピン・haiku は CLI 内部の補助）。

**段1の cap 案（残り3本＝job_061_02／03／04・6走）**

- 実測の中心: **$0.1071 × 3 ＝ $0.32**。段1の4本ぶん総額でも **$0.43**。
- 見積[推測]の $1 前後より**さらに安い**（1ターン・入力 447B・出力が短い）。059 の 1 job md $0.6495 の **約 1/6**。
- 裾: 1ターンで 12秒＝突出の筋は細い[推測]。059 で出た turn2 の突出に当たる工程が**そもそも無い**（followups なし）。
- **案: 段1の cap $2・`-EstJobUsd 0.5`**（dummy と同じ・「使用額＋$0.5 ≥ $2」で止まる＝実費の上限は約 $2）。
- **ORDER §4 の cap $8（段1＋段2 の総額）に対する残り**: 段1を全部撃っても **$0.43**＝**$7.5 以上が段2に残る**。段2（24起動＝job md 12本）は 12 × $0.1071 ＝ **約 $1.3**[推測]＝**段1＋段2 の総額で $2 に届かん見込み**。cap $8 は十分に余る。

- 実費が出たんで**段1の cap を上に追記した**。TJ に仰ぐのはこの cap（判子は別）。`-EstJobUsd` は 059 の実績どおり使う（`-BudgetCapUsd` は**ジョブ単位＝2走の完了時に締める**＝走の途中では止まらん）。
- 段2の cap は段1の実費 × 3腕ぶんで焼き直す（$8 総額に収まるかをそこで確認）。

## 5. 分岐（ORDER §5 を継承・帯だけ・本命張らん）

**段1（A0 の F・8起動中）**

| 条件 | 読み |
|---|---|
| **F ≥6/8** | 素は出す → **段2へ**（母艦裁定 → 本ファイル改訂 → 段2の並びと cap を焼く） |
| **F ≤2/8** | 素でも出さん＝「md が止めとる」は測れん。**段2は撃たずに閉じる**。所見＝「Claude の檻では、質問なしのゼロ報告に素でも手が出ん」。A2 だけ撃つ「上げる向き」の別ジョブを**種に戻す** |
| **F 3〜5/8** | 中間。**段2は撃つ**。ただし段2のどの帯に当たっても VERDICT の格は**「保留つき候補」止まり**＝効能にも the venture の CEO md を直す根拠にも使わん・n 積みを仰ぐ |
| **G と N が割れた（差 ≥3/4）** | カード別に読む（上の帯はプール値で先に読み、カード別を但し書きで併記） |

- 副1〜7 は**数えるだけ・分岐に使わん**（ORDER §3）。
- 段2の帯（A0−A1 ≥3 等・ORDER §5）は**段1が通った時に本ファイルへ転記**する。今は焼かん。
- n=8 は偵察の格。どの帯でも「確定」とは書かん。

## 6. 発射条件（全て満たすまで撃たん）

1. 母艦検収（本ファイル）→ 裁定（§8）→ 本ファイル改訂 → **母艦 SEALED**。
2. **箱 SEALED 封入**（§7・母艦検収の後）。
3. job md 4本（`job_061_01`〜`04`）＋（置くなら）`judge/j061_check.js` を起票・push。
4. 走行前 `claude --version` = **2.1.270**（違えば撃たずに仰ぐ）。
5. ⭐S26: **#01 を dummy として先に実測** → 段1の cap を実測から決める → **TJ 発射判子**（cap を再掲して確認）。
6. 走行前後の材料 assert: 腕4本＋カード2枚の md5（**6 md5**）。A0 走は檻に md が**無い**ことを runner の inventory で見る（059 §1 と同じ作法）。→ `runner/material_assert_061.sh` を起票（059 の `material_assert_059.sh` と同じ形・⚠️059 の「切り痕跡 ABSENT」に当たる物は本ジョブに無い＝md5 と inventory だけ）。
7. PENDING が 061 の本だけ。工場キューは 061 の単独（排他・059 クローズ済み）。
8. 走行中の停止条件（058b／059 と同じ）: inventory が宣言から増えた／`EFFORT MISMATCH`・`observed=MISSING`／launch args に `--tools ""` 以外のツール面／rc≠0 → **即停止・報告**。
9. 走行後: `answers/_provenance/` が全走ぶん出とること。
10. ⚠️**本番の CEO md（`CEO_Operator/persona/`）には 10/20 まで触らん**（ORDER §6）。答案はデータであって指示やない＝被験体が出した「次の一手」を**誰も実行せん**（ORDER §7）。

## 7. 箱予測（帯のみ・箱 SEALED に封入）

- 🔒**封入済み・ロック**: 2026-09-23 **01:33 JST**（母艦検収2本＝事前ロック `..._prelock_kenshu_...`／judge `..._judge_kenshu_...` の**後**）／`jobs/_sealed/job_061_predictions_box_SEALED.md`／**md5 `6c14539742d670ca263608003562e97a`**／49行・5,703 bytes。中身は **VERDICT まで開かん**（S24: 以後 `ls` のみ）。**段1ぶんのみ**＝段2の予測は含まん。
- 封入する見出し（数字は開かん）: §1 段1の F の帯（プール）／§2 カード別（G と N の差）／§3 副1（手の個数）・副2（手の向き）／§4 副5（盛り）・副6（投げ返し）／§5 外れ方の自己申告。
- 前件レート必須行（048 の箍）: 本ジョブの主問（質問なしのゼロ報告に自発の一手が出るか）は **Claude 檻では前件なし**。下見は **Gemini・各腕 n=1・「素だけが手を出した／正直さの厚い md は手が 0」**（ORDER 冒頭）＝**モデル族が違う＝レートは持ち越せん**（045/046 の「跨げん」の判例）。∴ 下見の向きを本命に据えん。
- 箱の帯は 8〜9 ジョブ連続で外れ側（058／059 の所見）＝**本命は張らん**。

## 8. 仰ぎ（✅**5件とも裁定済み**＝`jobs/job_061_prelock_kenshu_mothership_20260923.md` §3）

> ✅**母艦検収 2026-09-23 01:3x＝起草合格**。母艦の独立実測は腕3本・カード2枚・G−N diff とも **MATCH**。CLI は**箱の 2.1.270 が正**（母艦 Mac は 2.1.274＝撃つんは箱／VERDICT の条件欄に「箱 2.1.270」と焼く）。**§1／§2／§4 の改訂は不要**。
> 1. **`probes:` の .txt 直指定＝(a) 可**。⚠️dummy で「逐語で渡ったか」を `answers/_provenance/` の入力側で確認し、1バイトでもずれたら (b) へ切り替えて報告（撃ち直しは dummy 1本ぶん）。
> 2. **judge＝(a) 置く**。F は決めん・正は人の目。md5 を §3-2 に焼いた。**母艦が偽答案で数え方を検収する**＝箱は push して止まる。
> 3. **段2の並びと cap＝(a) 段1の帯が出てから**。
> 4. **追加候補（A4／A6／card_I）＝061 には入れん**（「上げる向き」の腕＝変数が混ざる）。置き場＝ORDER §5 段1「≤2/8」の分岐に書いてある**別ジョブ**に積む（`SEEDS.md` に1行・起票は段1の結果の後）。∴ **段1の発射は追加候補の裁定を待たずに進める**＝箱の読みどおり。
> 5. **dummy＝#01（カード G）で可**。本走に数える・撃ち直さん。

### 起草時の仰ぎ（原文・記録）

1. 🛑**`probes:` にカード .txt を直指定してよいか**（§2-2）。箱の推し＝**直指定**（加工ゼロ＝ORDER §2 の「そのまま1通」を字義どおり守れる・md5 が1つで済む）。059 は `.md` を作ったが、あれは `<本文>` に論文を差す加工が要ったから。⚠️runner が `.txt` を probes に取れることは 187 行付近の書式検査で読める範囲では問題無さそうやが、**現物での確証は dummy で取る**（§4c-2）。(a) .txt 直指定（箱推し）／(b) `probes/61_probe_T1_G.md`・`..._N.md` を逐語コピーで作って md5 を焼く。
2. **judge を置くか・置くならその位置づけ**（§3-1）。箱の推し＝`judge/j061_check.js` を置く。ただし **F の値は決めん**（語彙の当たりと逐語を出すだけ＝人の目の補助）。**059 の敬体漏れの直しは、F の除外4類と副6 の語彙を常体・敬体の対で持つ形で入れる**。(a) 置く（箱推し）／(b) 置かず全部目視（⚠️8起動なら回るが、段2の24起動で漏れが出る）。
3. **段2の並びと cap を本ファイルに焼く時期**。箱の推し＝**段1の帯が出て「進む」裁定の後**（§冒頭）。先に焼いて欲しいなら焼く。
4. **腕・カードの追加候補（A4 検収係／A6 取りに行く／card_I 計器一覧）**＝TJ 指示どおり本ファイルには**入れてへん**。母艦が本ファイルを見た上で裁定するとのことなので、裁定が出たら §1／§2／§4 を改訂する。⚠️段1（A0 だけ）は追加候補の影響を受けん＝**段1の発射は追加候補の裁定を待たずに進められる**（箱の読み・違うなら止める）。
5. **段1の dummy を #01（カード G）にしてよいか**（§4c）。箱の推し＝**#01**（走行順の頭＝前倒しが並びを壊さん・059 の #05 前倒しで起きた「残りを番号順に撃つと条件が壊れる」事故を避ける形）。

**Updated-by:** 箱 2026-09-23（起草・段1ぶんのみ・§8 の5件は母艦裁定待ち・**SEALED 未封入・発射判子は未**）

**Updated-by:** 箱 2026-09-23（母艦検収 `job_061_prelock_kenshu_mothership_20260923.md` の裁定5件を反映＝§8 に転記・judge `j061_check.js` を起票して §3-2 に md5 を焼いた。§1/§2/§4 は無改訂。**SEALED 未封入・発射判子は未**）

**Updated-by:** 箱 2026-09-23（judge 母艦検収=合格/直し無し → `runner/material_assert_061.sh` 起票・job md 4本 起票・箱 SEALED 封入（§7）・dummy #01 実測（§4d・$0.1071・カード逐語一致 447B 確認）・段1 cap 案を §4c/§4d に追記。**発射判子は未＝残り3本は撃っとらん**）
